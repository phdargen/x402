---
"@x402/concordium": minor
---

Declare `upfront` payment flow support on Concordium `exact` server schemes. `authorization` remains the default; servers opt in per route via `accepts.extra.paymentFlow`.
