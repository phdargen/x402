# x402 Custom Client Example - Environment Variables

# At least one private key is required (EVM or SVM)
# EVM private key (with 0x prefix) - for Base, Ethereum, etc.
EVM_PRIVATE_KEY=0x...

# SVM private key (base58 encoded) - for Solana
SVM_PRIVATE_KEY=...

# Resource server URL (without trailing slash)
RESOURCE_SERVER_URL=http://localhost:4021

# Endpoint path to request (with leading slash)
ENDPOINT_PATH=/weather
