# x402 Client Examples

Client examples now live in [`../scripts/clients/`](../scripts/clients/). See [`../scripts/README.md`](../scripts/README.md) for how to run them.

Quick start:

```bash
cd ../scripts
cp .env.example .env
pnpm exec tsx clients/http/fetch.ts
```
