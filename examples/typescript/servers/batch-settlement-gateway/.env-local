EVM_ADDRESS=0x...
FACILITATOR_URL=http://localhost:4022

# Signs GatewayClaimAuthorization; also deterministically derives M test payout addresses
EVM_RECEIVER_AUTHORIZER_PRIVATE_KEY=0x...

# Number of simulated server routes for the gateway load client
M_SERVERS=4
