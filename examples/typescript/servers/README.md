# x402 Server Examples

Server examples now live in [`../scripts/servers/`](../scripts/servers/). See [`../scripts/README.md`](../scripts/README.md) for how to run them.

Quick start:

```bash
cd ../scripts
cp .env.example .env
pnpm exec tsx servers/http/express.ts
```
