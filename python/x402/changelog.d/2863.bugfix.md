Defer batch-settlement channel reservation until after successful verify, validate canonical channel ids with path confinement, and allow after-verify hooks to abort with `after_verify_aborted` cleanup.
